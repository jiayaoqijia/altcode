# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# Reference checkouts — kept locally for design study but never
# committed (open-source policy: no submodule pointers in commits).
# The directories may be cloned manually with:
#   git clone <url> vendor/<name>
# Listed explicitly so a stray `git add vendor` doesn't sneak them in.
vendor/
claude-code/
opencode/

# Build artifacts
dist/
build/
out/
target/
*.o
*.so
*.dylib

# Screenshot/debug artifacts at repo root — TUI captures, Playwright bug
# series, etc. Real docs imagery lives under docs/images/ (negation below).
/*.png
!/docs/images/*.png

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Secrets & credentials — NEVER commit these
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.p8
*.der
*.crt
*.cer
credentials.json
service-account*.json
*-credentials.json
*-key.json
*.secret
*.secrets
secrets/
.secrets/
.credentials/
token.json
tokens.json
oauth2.keys.json
gcp-key.json
firebase-adminsdk*.json
*_rsa
*_rsa.pub
*_ed25519
*_ecdsa
id_rsa
id_ed25519
.ssh/
*.gpg
*.asc
.netrc
.npmrc
.pypirc
.docker/config.json
kubeconfig
.kube/config

# Testing & coverage
coverage/
.nyc_output/
*.lcov
htmlcov/

# Logs
*.log
logs/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan

# OS
.DS_Store
Thumbs.db
desktop.ini

# Package managers
.pnpm-store/
.yarn/
*.tgz

# AI agent files (local only)
.claude/settings.local.json
AGENTS.local.md
PLAN.md
.local/

# Model-generated files from benchmarks
*.go.bak
main.go.bak

# Model-generated files from benchmark tests (DO NOT COMMIT)
internal/main.go
internal/stack*.go
internal/lru*.go
internal/middleware*.go
internal/ratelimit/
internal/reverse_test.go
internal/rate_limiter*.go
internal/howmanytimes.go
internal/example_*.go
internal/*_example.go
lru_cache.go

# Aggressively ignore model-generated packages
internal/lru/
internal/stack/
internal/middleware/
internal/ratelimit/
internal/datastructures/
internal/stringxor.go
internal/main.go
internal/hascloseelements.go
internal/prime.go

# Model-generated benchmark junk
internal/ratelimit/
internal/counter*.go
internal/filter.go
internal/generics*.go
internal/math*.go
internal/reverse.go
internal/user*.go
/middleware*.go
internal/ratelimit/
internal/reverse_test.go
internal/stack*.go
internal/lru_cache*.go
internal/middleware*.go
internal/cli.go
internal/filter.go
internal/math*.go
internal/user*.go
internal/example_*.go
.altcode/workspace/
,
", "

# OSS hygiene (added during open-source cleanup):
# - Auto-memory directory (per-conversation state, never relevant in commits)
# - Playwright MCP cache directory
# - Repo-root binary builds (Makefile already builds to dist/)
# - Backup / disabled files left behind by editors and revert flows
.claude/memory/
.playwright-mcp/
/altcode
/hello
*.bak
*.bak2
*.bak3
*.disabled
# Benchmark detritus that has bitten us repeatedly — pin the names so
# `go test ./...` from a re-run can't sneak them back in.
internal/binarysearch.go
internal/cache/
internal/fib/
internal/generics/
internal/math/
internal/mathutil/
internal/prime/
internal/util/
internal/COUNTER_FIX.md
MIDDLEWARE_GUIDE.md
/cli/
/cmd/cli/
/cmd/cli-example/
/cmd/flagcli/
/cmd/hello/
/pkg/middleware/
